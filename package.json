{
  "name": "interappcom",
  "version": "0.0.1",
  "description": "To allow communication between application",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "moment": "^2.22.0"
  },
  "babel": {
    "presets": ["env"]
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1"
  },
  "scripts": {
    "build": "babel src -d lib"
  }
}
