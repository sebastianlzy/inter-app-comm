language: node_js
node_js:
  - "node"
script:
  - yarn test
deploy:
  provider: pages
  script: yarn start
  skip-cleanup: true
  github-token: $GITHUB_TOKEN  # Set in travis-ci.org dashboard, marked secure
  on:
    branch: gh-pages
